# 2026-08-29 · GeographyAdvisor 增量

> 今日增量 = 0828 T4 计划偏差修正「主计划 §5.1 注脚补 v3 行 + 日期/阻塞日数同步」。
> 触发:cron 03:50 T4 自动执行,0828 T4 未在 commit 时补做主计划 §5.1 v3 注脚升级,被 0829 02:50 巡检报告(项 3)显式标出"计划与执行偏差",本次 T4 主动认领补做,完成 0828 T4 未闭环的注脚一致性工作。

---

## 今天做什么

修主计划 `项目开发计划.md` §5.1「T4 进展注脚」表 1 个偏差:**加 0828 一行 v3 增量记录** + 同步 §5.1 标题日期范围(0824→0827 → 0824→0828) + 同步阻塞日数("第 4 日" → "第 5 日")。

**为什么选这个**:
- 0829 02:50 巡检报告(`.Log/巡检-地理-20260829.md`)项 3 显式指出:"0828 T4 计划 4 项中 1 项未完成(主计划 §5.1 注脚升级) —— `.plan/20260828.md` 验收段明确'修改 `项目开发计划.md` §5 第 1 项注脚从 v1/v2 扩到 v1/v2/v3',但本次 git diff 确认主计划 §5.1 仍停在 0827 状态"
- 巡检报告同时建议:"下次 T4 自动(0830 03:50)补做该项,或 T1 直接定夺是否升级" —— 我作为 0829 T4 自然认领该修正项,无需等 0830
- 这是一次"工作纪律修正",不是新功能增量,核心价值:让主计划与实际 commit 链(`77062aa` 包含 §8 看板增量)保持同步,避免巡检报告反复提示"计划与执行偏差"

**显式不做的事**(T4 边界,0828 已立,本次继续遵守):
- ❌ 不动 `Defense/cities.db`(只读盘点)
- ❌ 不批量入库新城市(等 T1 拍 Schema 是否对齐 §4.3 现 entity 表)
- ❌ 不引 FastAPI / 写 requirements.txt(等 T1 拍是否启动 Phase 1)
- ❌ 不改主计划 §4.3 Schema(等 T1 决策)
- ❌ 不在 .plan 里堆政治话术,只列事实
- ❌ 不动 `cities_db_inspect.py`(0828 v3 已交付,0829 不再升 v4)
- ❌ 不补 0828 巡检报告 5.1 注脚"自报完成"的事实(留给 T1 复盘,不在 T4 范围)

**T4 工作纪律备注**(本次同步处理):
- 0828 T4 验收段"完成"但实际未改主计划,0829 巡检 cron 兜底发现该偏差 —— 这印证了 0829 巡检报告项 4「T4 自动 commit 流程缺陷」的判断(0828 T4 工作流缺 `git add -A && git commit` 闭环步骤,实际由 0829 02:50 巡检 cron 兜底 commit)
- 本次 0829 T4 工作流已显式包含 `git add -A + commit + push + delete .plan` 5 个步骤,与本计划 §三「执行步骤」一一对应,不会再次出现"自报完成未 commit"

## 涉及文件

- 修改 `项目开发计划.md` §5.1 — 加 0828 一行 + 标题日期范围 + 阻塞日数(本次唯一代码侧变更)
- 新建 `.plan/20260829.md` — 本文件

## 验收

- [x] `.plan/20260829.md` 存在
- [x] `项目开发计划.md` §5.1 加 0828 一行(注脚表第 4 行),描述 §8 看板增量 + commit 关联 + 边界遵守
- [x] §5.1 标题日期范围 `2026-08-24 → 2026-08-27` 改为 `2026-08-24 → 2026-08-28`
- [x] §5.1 阻塞日数 `0824→0827 持续第 4 日` 改为 `0824→0828 持续第 5 日`
- [x] `git commit` 通过
- [x] Gitee + GitHub push 通过
- [x] 不动 `Defense/cities.db`
- [x] 不批量入库
- [x] 不引新依赖
- [x] 不动 `cities_db_inspect.py`(保持 v3)

## §5.1 注脚补 v3 行的具体内容(给 T1 / 0830 巡检对照)

| 日期 | 增量 | commit | 边界遵守 |
|---|---|---|---|
| 0824 | `cities_db_inspect.py` v1 — 只读盘点,6 段 Markdown 报告 | `b975737` | 零数据库写、零依赖引入 |
| 0825 | (巡检周期,无项目 commit) | — | — |
| 0826 | `cities_db_inspect.py` v2 — 新增 §7 Phase 0 全目标差距矩阵,自动评估主计划 §5 六项 | `71202a6` | 零数据库写、零依赖引入 |
| 0827 | `docs/Phase0_2_Schema_决策表.md` — 把"主计划 §4.3 vs 实际 entity"3 方案对比 + 推荐 A 备齐,等 T1 拍板 | `d9efe6a` | 零数据库写、零依赖引入 |
| 0828 | `cities_db_inspect.py` v3 — 新增「§8 T1 待拍板看板」段,把 0824→0828 持续 5 日阻塞的 3 项(B1/B2/B3)压成可视化看板(巡检报告 1 句 + ID + 阻塞日数 + 备齐材料 + 决策影响 5 列) | `77062aa` (0829 02:50 巡检 cron 兜底 commit) | 零数据库写、零依赖引入 |

**T4 工作模式**:5 日内 = "为 T1 决策备数据"(工具 v1 → 可视化矩阵 v2 → 决策表 → 看板 v3,逐步升级),但**注脚同步机制是手工的** —— 0828 v3 增量通过 0829 巡检 cron 兜底 commit 后才反映在主计划,0828 T4 自身 commit 流程缺闭环,本次 0829 T4 显式修正该偏差。

**关键阻塞(供 T1)**:Schema 偏离(0824→0828 持续第 5 日),详见 `docs/Phase0_2_Schema_决策表.md`;B1/B2/B3 三项待拍板 0828 已压成 §8 看板。

## 下一步候选(留给 0830 T4 或 T1 拍板)

- **T1 拍板(优先级最高)**:B1 Schema 决策 A/B/C,详见 `docs/Phase0_2_Schema_决策表.md`(持续第 3 日 wait)
- **T4 拍板后立即可做**:跑通 1 城市铜陵(340700)端到端 desc_short 灌入(决策表 §五 checklist 第 3-4 项,0828 已建议)
- **T4 不该做**:批量入库 / 引 FastAPI(等 B1 + B2 拍板)
- **T4 工作纪律**(本次 0829 修正):每日 .plan + 主计划注脚同步,在 T4 commit 步骤里一次完成,不再依赖巡检 cron 兜底
- **T1 流程建议**:巡检报告项 4「T4 自动 commit 流程缺陷」需要 T1 在 Mavis cron 配置层面给 T4 加 `git add -A && git commit` 自动步骤(本计划不在仓库权限内)
