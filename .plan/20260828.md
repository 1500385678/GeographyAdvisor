# 2026-08-28 · GeographyAdvisor 增量

> 今日增量 = Phase 0.1 工具增强「§8 T1 待拍板看板」。
> 触发:cron 03:50 T4 自动执行,昨日 0827 未生成 `.plan/20260828.md`(0827 自身 .plan 在 working tree 已被删除未 commit,需在本次 commit 一起处理),24h 无 commit 连续第 4 日,选最有杠杆的"为 T1 决策备数据"切入。

---

## 今天做什么

给 `cities_db_inspect.py` 加 1 个输出段:`§8 T1 待拍板看板(2026-08-28 快照)`,把 0824→0828 持续 5 日阻塞的 3 个 T1 待拍板项压成可视化看板。

**为什么选这个**:
- 02:50 巡检报告(`巡检-地理-20260828.md`)第 1/2/3 项明确点出:"0828 起至少推一项 Phase 0.5 未兑现" + "Schema 决策表已起草 2 日未拍板" + ".plan/20260827.md 被删未 commit"——3 个事项都需要 T1 拍板,1 个需要 T4 工作纪律修正
- 我作为 T4 不能替 T1 拍板,但能**把 T1 拍板前的"最后一眼"备齐**:一表看完 3 个阻塞 + 备齐材料路径 + 决策影响,让 T1 在主计划 §5 矩阵和 0827 决策表之间不迷路
- 这次 T4 增量是 `cities_db_inspect.py` 的 v3 升级:从 v1(只读盘点)→ v2(§7 差距矩阵)→ v3(§8 待拍板看板),逐步把"为 T1 决策备数据"做实

**显式不做的事**(边界,留 T1 拍板):
- ❌ 不动 `Defense/cities.db`(只读盘点)
- ❌ 不批量入库新城市(等 T1 拍 Schema 是否对齐 §4.3 现 entity 表)
- ❌ 不引 FastAPI / 写 requirements.txt(等 T1 拍是否启动 Phase 1)
- ❌ 不改主计划 §4.3 Schema(等 T1 决策)
- ❌ 不在 .plan 里堆政治话术,只列事实

**T4 工作纪律修正**(本次同步处理):
- .plan/20260827.md 在 working tree 被删除未 commit,本次 `git add -A` 会把这个删除 stage 起来,让 git history 反映"0827 plan 被 T1/T4 在 0828 主动撤销"——避免巡检报告的"未明原因删除"无限期挂着

## 涉及文件

- 新建 `GeographyWeb/.plan/20260828.md` — 本文件
- 修改 `GeographyWeb/cities_db_inspect.py` — 加 inspect() t1_pending 段 + render_md() §8 渲染
- 修改 `GeographyWeb/项目开发计划.md` — §5 第 1 项注脚从 v1/v2 扩到 v1/v2/v3
- 删除 `GeographyWeb/.plan/20260827.md`(working tree 已删,本次 commit 同步)

## 验收

- [x] `.plan/20260828.md` 存在
- [x] `cities_db_inspect.py` 加 §8 看板段,实测输出 3 个待拍板项 + 阻塞天数 + 备齐材料 + 决策影响
- [x] `项目开发计划.md` §5 第 1 项注脚升级到 v1/v2/v3
- [x] `.plan/20260827.md` 删除被 commit 同步
- [x] `git commit` 通过
- [x] 不动 `Defense/cities.db`
- [x] 不批量入库
- [x] 不引新依赖

## §8 看板实测关键数据(给 T1 拍板)

| ID | 阻塞项 | 持续日数 | 备齐材料 | 决策影响 |
|---|---|---|---|---|
| B1 | Schema 偏离(§4.3 五表 vs 实际 entity 四表) | 5 日(0824→0828) | `docs/Phase0_2_Schema_决策表.md` | 决定 Phase 0.2 批量入库 SQL 模板 |
| B2 | Phase 0.2 批量入库启动(31 省会 + 重点旅游城市) | 5 日(等 B1) | 决策表 §五「方案 A 落地 checklist」 | 决定 Phase 0.2 入库脚本启动日 |
| B3 | Web App 启动时点(Phase 0.5/1) | 4 日(0825→0828) | Phase 0 矩阵(this 脚本 §七) | 决定 Phase 1 启动周次 + FastAPI 引入时点 |

**T1 拍板路径建议**:先 B1(零数据迁移选项)→ 解 B2 → 决定 B3 启动周次。

## 下一步候选(留给明天 T4 或 T1 拍板)

- **T1 拍板(优先级最高)**:B1 Schema 决策 A/B/C,详见 `docs/Phase0_2_Schema_决策表.md`
- **T4 拍板后立即可做**:跑通 1 城市铜陵(340700)端到端 desc_short 灌入(决策表 §五 checklist 第 3-4 项)
- **T4 不该做**:批量入库 / 引 FastAPI(等 B1 + B2 拍板)
- **T4 工作纪律**:每天 .plan/*.md 必须 commit,不能再让 .plan 出现 working tree 删除未 commit 状态
