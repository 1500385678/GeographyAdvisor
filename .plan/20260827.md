# 2026-08-27 · GeographyAdvisor 增量

> 今日增量 = Phase 0.2 决策数据「Schema 偏离决策表」。
> 触发:cron 03:50 T4 自动执行,从昨日 T4 `.plan/20260826.md` "T4 可做"清单 + 0827 巡检"三连零变化趋势" 推断。

---

## 今天做什么

写一份 `docs/Phase0_2_Schema_决策表.md`,把 0824 至今悬而未决的"主计划 §4.3 五表 vs 实际 entity 四表"Schema 偏离问题,做成可入库的决策表,供 T1 拍板直接消费。

**为什么选这个**:
- 0825/0826/0827 三份巡检**反复**把"Schema 偏离悬而未决"列为 Top 3 阻塞
- 但 T1 至今未拍板,T4 又不能越界改 schema 或改主计划
- 我能做的最有杠杆的事就是**把决策数据备齐**:3 个方案(改主计划 / 改 schema / 双库并跑)+ 优缺点对比 + 推荐方案
- 同时也延续昨日 .plan/20260826.md "T4 可做 = 决策数据备齐"的工作流

**显式不做的事**(沿用 .plan/20260826.md 边界,继续不破):
- ❌ 不动 `Defense/cities.db`(只读盘点)
- ❌ 不批量入库新城市(等 T1 拍 Schema)
- ❌ 不引 FastAPI / 写 requirements.txt(等 T1 拍是否启动 Phase 1)
- ❌ 不改主计划 §4.3 Schema(等 T1 决策)
- ❌ 不改 `cities_db_inspect.py`(昨天的 v2 增强够用,不再加 §8)

## 涉及文件

- 新建 `docs/Phase0_2_Schema_决策表.md` — 3 方案对比 + 推荐 A + 落地 checklist
- 修改 `项目开发计划.md` — §5 末尾加 0827 进展注脚(本文档存在 + 决策表位置)

## 验收

- [x] `docs/Phase0_2_Schema_决策表.md` 已新建(7188 字节,含 3 方案 + 矩阵 + 推荐理由)
- [x] `项目开发计划.md` §5 末尾加 0827 进展注脚
- [x] `.plan/20260827.md` 存在(本文件)
- [x] `git commit` 通过
- [x] 不动 `Defense/cities.db`
- [x] 不批量入库
- [x] 不引 FastAPI
- [x] 不改 cities_db_inspect.py

## 决策表关键结论(给 T1 决策)

| 方案 | 核心动作 | 数据迁移 | 动 Defense | Phase 0.2 启动 |
|---|---|---|---|---|
| **A 改主计划(推荐)** | 改 §4.3 为 4 表 + 多态 | 零 | ❌ 否 | **当周** |
| B 改 schema | 在 cities.db 新建 5 表 | 需迁移 | ✅ 是 | 1-2 周后 |
| C 双库并跑 | 新建 web.db + ETL | 需 ETL | ❌ 否 | 1 周后 |

**推荐 A**:理由 = 零迁移 / 不动 Defense / 多态是张勇设计语言。

## 与昨日 T4 工作流的连续性

| 日期 | 增量 | 杠杆点 |
|---|---|---|
| 0824 | cities_db_inspect.py v1 | 巡检基础 |
| 0826 | cities_db_inspect.py v2(§7 矩阵) | 决策数据可视化 |
| **0827(今日)** | **Schema 决策表** | **T1 拍板直接消费** |

3 日内 T4 工作模式 = "为 T1 决策备数据":从"工具 → 可视化矩阵 → 决策表"逐步升级。

## 下一步候选(留给明天 T4 或 T1 拍板)

- **T1 拍板(优先级最高)**:在决策表 §六 标注"已选 A/B/C + 决定日期",T4 据此执行方案 A 落地 checklist
- **T4 可做(等拍板后)**:方案 A 落地 — 改主计划 §4.3 + 写 `tools/import_city_profile.py` + 跑铜陵 1 城端到端
- **T4 可做(独立)**:更新 `README.md` Phase 0 状态行(把 1/6 → 反映 T4 工作流)
- **T4 不该做**:批量入库 200 城 / 引 FastAPI / 改 Defense/cities.db
