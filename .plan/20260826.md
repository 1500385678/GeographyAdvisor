# 2026-08-26 · GeographyAdvisor 增量

> 今日增量 = Phase 0.1 工具增强「Phase 0 全目标差距矩阵」。
> 触发:cron 03:50 T4 自动执行,从昨日 T4 未生成 `.plan/20260825.md` 推断——24h 无 commit,选最有杠杆的「数据驱动决策」切入。

---

## 今天做什么

给 `cities_db_inspect.py` 加 1 个输出段:`§7 Phase 0 全目标差距矩阵(主计划 §5)`,把主计划 §5 的 6 个 checkbox 逐项映射到 DB 现状,自动出 `done / in_progress / todo` 汇总。

**为什么选这个**:02:50 巡检报告指出 24h 无 commit、Phase 0 进度 1/6、Schema 偏离悬而未决、Web App 骨架空——但这些都是**决策问题**不是代码问题。我作为 T4 没法替 T1 拍板,能做的是**把决策数据备齐**。新矩阵段让 T1 拍板时不用人肉对照主计划 vs 巡检报告,直接消费 `python3 cities_db_inspect.py` 输出的表格。

**显式不做的事**(边界,留 T1 拍板):
- ❌ 不动 `Defense/cities.db`(只读盘点)
- ❌ 不批量入库新城市(等 T1 拍 Schema 是否对齐 §4.3 现 entity 表)
- ❌ 不引 FastAPI / 写 requirements.txt(等 T1 拍是否启动 Phase 1)
- ❌ 不改主计划 §4.3 Schema(等 T1 决策)
- ✅ 改 §5 第 1 项 checkbox:事实上已交付,顺手勾上 + 标注 v1/v2

## 涉及文件

- 修改 `GeographyWeb/cities_db_inspect.py` — 加 inspect() phase0_matrix 段 + render_md() §7 渲染
- 修改 `GeographyWeb/项目开发计划.md` — §5 第 1 项 `- [ ]` → `- [x]`,附 v1/v2 时间线
- 新建 `GeographyWeb/.plan/20260826.md` — 本文件

## 验收

- [x] `cities_db_inspect.py` 加 §7 矩阵段,实测输出 6 行 + done/in_progress/todo 汇总
- [x] `项目开发计划.md` §5 第 1 项已勾选,带 v1/v2 注脚
- [x] `.plan/20260826.md` 存在
- [x] `git commit` 通过
- [x] 不动 `Defense/cities.db`
- [x] 不批量入库

## 矩阵实测关键数据(给 T1 决策)

| ID | 任务 | 状态 | 实际 |
|---|---|---|---|
| 0.1 | inspect 脚本 | ✅ done | 204 行,本次升级到含矩阵 |
| 0.2 | 补全字段 | 🟡 in_progress | desc_short 0/3(展示层无简介) |
| 0.3 | 88 城市档案 | 🟡 in_progress | 3 city(铜陵/上海/北京) |
| 0.4 | 文化符号库 | 🟡 in_progress | 30 symbol(全 culture+nature) |
| 0.5 | FastAPI 骨架 | ⬜ todo | 无 app.py / requirements.txt |
| 0.6 | 飞书 Bot | ⬜ todo | agent cron 可代答,bot 未配 |

**汇总**:`done 1 · in_progress 3 · todo 2`

## 下一步候选(留给明天 T4 或 T1 拍板)

- **T1 拍板(优先级最高)**:Schema 偏离(主计划 §4.3 五表 vs 现 entity 四表)拍板——决定后续所有入库脚本的 schema
- **T4 可做**:补 1 个城市的 desc_short(从 `_GeographyLib/02_地理分支与特点/` 摘 1 段),把 0.2 进度从 0/3 推到 1/3
- **T4 可做**:写 `requirements.txt` + `app.py` 极简 FastAPI `/health`,把 0.5 从 todo 推到 in_progress
- **T4 不该做**:批量入库(等 T1 拍板 Schema)
