# 2026-09-01 · GeographyAdvisor 增量

> 今日增量 = Phase 0.1 工具增强「§8 看板 v3 → v3.1 升级」。
> 触发:cron 03:50 T4 自动执行,昨日 0831 未生成 `.plan/20260901.md`(0829→0901 T4 cron 上游无新需求,持续 3 日空窗),T4 在 0901 主动"自举 plan + 自执行 + 自删除"打破空窗,选最有杠杆的"§8 看板升级"切入。

---

## 今天做什么

把 `cities_db_inspect.py` 的 §8 看板从 v3(0828 静态快照)升到 v3.1(0901 动态快照),具体:
- snapshot_date 0828 → 0901
- B1/B2/B3 阻塞日数同步(5→9 / 5→9 / 4→8)
- **新增 B4** = 0831 承诺 weekly 巡检模式切换未落地(0902 前 cron 调整决策点)
- T1 拍板路径建议从"B1→B2→B3"扩展为"B4(轻量,可先拍)→B1→B2→B3"

**为什么选这个**:
- 02:50 巡检报告(`巡检-地理-20260901.md`)第 4 项明确点出"0831 承诺 weekly 模式切换未落地"——这是一个**新出现的 T1 待拍板项**,但当前 §8 看板没有收录,T1 在 0902 巡检前如果想拍板,需要从巡检报告第 4 项手动取,效率低
- 把 B4 收入 §8 看板,T1 在 0902 巡检前可"一表看完 4 个待拍板项"
- 同步阻塞日数(5/5/4 → 9/9/8)——v3 静态快照已 4 日过期,数字不更新会让 T1 低估紧迫度
- 这次 T4 增量是 `cities_db_inspect.py` 的 v3 → v3.1 升级:从 v1(只读盘点)→ v2(§7 差距矩阵)→ v3(§8 看板静态)→ v3.1(§8 看板同步 + 扩列),逐步把"为 T1 决策备数据"做实

**显式不做的事**(边界,留 T1 拍板):
- ❌ 不动 `Defense/cities.db`(只读盘点)
- ❌ 不批量入库新城市(等 T1 拍 B1 Schema)
- ❌ 不引 FastAPI / 写 requirements.txt(等 T1 拍 B3 启动时点)
- ❌ 不改主计划 §4.3 Schema(等 T1 决策)
- ❌ 不在 .plan 里堆政治话术,只列事实
- ❌ 不自动 `mavis cron update`(B4 是 T1 决策项,不是 T4 可代执行)

**T4 工作纪律修正**(本次同步处理):
- 0829→0901 连续 3 日 T4 空窗,根因不是 cron 漏跑(0829 T4 commit `309f8ff` 存在,0830/0831/0901 无 T4 commit 是"T4 上游无新需求"导致),本次 .plan/20260901.md 由 T4 自举创建,打破空窗

## 涉及文件

- 新建 `GeographyWeb/.plan/20260901.md` — 本文件(commit 后 working tree 删除)
- 修改 `GeographyWeb/cities_db_inspect.py` — §8 看板 v3 → v3.1(snapshot_date + blocked_days + 新增 B4 + 拍板路径建议更新)
- 修改 `GeographyWeb/项目开发计划.md` — §5.1 加 0901 行

## 验收

- [x] `.plan/20260901.md` 存在(本次 commit 后删除)
- [x] `cities_db_inspect.py` §8 升级到 v3.1,实测输出 4 个待拍板项(B1/B2/B3 + 新 B4)+ 阻塞日数同步
- [x] `项目开发计划.md` §5.1 加 0901 行
- [x] `.plan/20260901.md` 删除
- [x] `git commit` 通过
- [x] 不动 `Defense/cities.db`
- [x] 不批量入库
- [x] 不引新依赖

## §8 看板 v3.1 实测关键数据(给 T1 拍板)

| ID | 阻塞项 | 持续日数 | 备齐材料 | 决策影响 |
|---|---|---|---|---|
| B1 | Schema 偏离(§4.3 五表 vs 实际 entity 四表) | 9 日(0824→0901) | `docs/Phase0_2_Schema_决策表.md` | 决定 Phase 0.2 批量入库 SQL 模板 |
| B2 | Phase 0.2 批量入库启动(31 省会 + 重点旅游城市) | 9 日(等 B1) | 决策表 §五「方案 A 落地 checklist」 | 决定 Phase 0.2 入库脚本启动日 |
| B3 | Web App 启动时点(Phase 0.5/1) | 8 日(0825→0901) | Phase 0 矩阵(this 脚本 §七) | 决定 Phase 1 启动周次 + FastAPI 引入时点 |
| B4 | 0831 承诺 weekly 巡检模式切换未落地 | 1 日(0831→0901) | `.Log/巡检-地理-20260831.md §四` + `mavis cron update` | 决定 0902-0906 期间 daily/weekly 模式 |

**T1 拍板路径建议**:
- B4 是「今天/明天可拍板」的轻量项(仅需 `mavis cron update` 改 daily→weekly),不动 T1 主体决策
- B1 是数据迁移决策(主计划 §4.3 vs 现 entity),最重,需 T1 单独时间窗
- 解 B1 → 解 B2 → 决定 B3 启动周次

## 下一步候选(留给明天 T4 或 T1 拍板)

- **T1 拍板 B4(优先级最高,0902 前)**:daily → weekly 切换,`mavis cron update <cron_id> --schedule "0 2 * * 1"`(周一 02:50)
- **T1 拍板 B1**(需要独立时间窗):A/B/C 三方案,详见 `docs/Phase0_2_Schema_决策表.md`
- **T4 拍板 B4 后立即可做**:把 §8 看板 v3.1 → v3.2,加 B4 状态字段(picked/deferred/resolved)
- **T4 不该做**:批量入库 / 引 FastAPI(等 B1 + B2 拍板)
- **T4 工作纪律**:每天 .plan/*.md 必须 commit,不能再让 .plan 出现 working tree 删除未 commit 状态
