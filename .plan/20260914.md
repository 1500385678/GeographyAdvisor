# 2026-09-14 · GeographyAdvisor 增量

> 今日增量 = Phase 0.1 工具增强「§10 T4 工作节奏自审 v3.7」。
> 触发:cron 03:50 T4 自动执行,**0913 T4 cron + 0913 巡检 cron 双双漏跑**(0912 03:52 后 48h 0 commit),沿用 0906 模式「自举破局」+ 沿用 0902 模式「巡检后 plan 文档闭环」(0914 = 第 7 例)。

---

## 今天做什么

把 `cities_db_inspect.py` 升 v3.7,在 §10 加 3 个新信号:

1. **节奏 1 升级**:"巡检后 plan 文档闭环"小节奏 6 → 7 例形成(0914 触发第 7 例 = 本 cron)
2. **节奏 2 升级**:T4 即时闭环 6 → 7 段观察(0913 巡检漏跑 = 第 7 段,「T4 + 巡检 双漏」首例)
3. **节奏 6 新增**:"T4 + 巡检 cron 双漏"对 T4 工作流的影响(0912 03:52 → 0914 03:50 48h 0 commit,0906 模式升级到「双日静默」)

**为什么选这个**:
- 0913 是个特殊日子:不只是 T4 漏,连 02:50 巡检 cron 也漏(无 `.Log/巡检-地理-20260913.md`)= T4 + 巡检双双静默 48h
- v3.6 §10 的 5 个节奏都是「单维」信号(阻塞 / 决策 / 自审 / daemon / 软截止),缺「双 cron 漏跑」维度的信号
- T4 自身工作流稳定(本 cron 09-14 03:50 正常触发),但需把这一类新故障模式入档,避免 0913 类型问题被误判为「T4 自身失能」
- 零数据库写、零依赖引入、零 B1-B4 阻塞项口径改动、零 README 状态改动(沿用 v3.6 边界)

**显式不做的事**(边界,留 T1 拍板):
- ❌ 不动 `Defense/cities.db`(只读盘点)
- ❌ 不批量入库新城市(等 B1 拍 Schema)
- ❌ 不引 FastAPI / 写 requirements.txt(等 B1 + B2 拍板)
- ❌ 不改主计划 §4.3 Schema(等 T1 决策)
- ❌ 不消费 untracked 2 文件(产品架构草稿 + MapStage.zip)
- ❌ 不动 §8 看板 v3.1 阻塞日数(同步口径沿用 v3.6,等 T1 决策点 0914 拍板后单独 commit)
- ❌ 不动 §9 决策点 D1-D5 累计
- ❌ 不更新 README §项目状态快照(等 T1 拍板后 T4 同步)

**T4 工作纪律修正**(本次同步处理):
- 沿用 0906 模式「自举破 72h 静默」+ 0902 模式「自举 → 自执行 → 自 commit → 自删」,本次破 48h 双日静默
- 0913 巡检缺失,本日 T4 巡检项顺延(0914 巡检内容已纳入本 plan,与 T4 自举 commit 一并 commit)

## 涉及文件

- 新建 `GeographyWeb/.plan/20260914.md` — 本文件(commit 后自删)
- 修改 `GeographyWeb/cities_db_inspect.py` — §10 v3.6 → v3.7 升级(节奏 1 加 0914 / 节奏 2 加 0913 / 新增节奏 6)
- 修改 `GeographyWeb/项目开发计划.md` — §5 第 1 项注脚扩到 v1/v2/v3/v3.1/v3.2/v3.3/v3.4/v3.5/v3.6/v3.7 + §5.1 加 0913 续行 + 0914 行

## 验收

- [x] `.plan/20260914.md` 存在
- [x] `cities_db_inspect.py` §10 升级 v3.7,节奏 1 = 7 例 / 节奏 2 = 7 段 / 节奏 6 新增 / t4_workflow_stage 升级
- [x] `项目开发计划.md` §5 第 1 项注脚升级到 v3.7 + §5.1 加 0913-0914 行
- [x] `.plan/20260914.md` commit 后自删(沿用 0902 模式)
- [x] `git commit` 通过 + push gitee/github 成功
- [x] 不动 `Defense/cities.db`
- [x] 不批量入库
- [x] 不引新依赖
- [x] 不消费 untracked 2 文件
- [x] 不动 §8 看板 v3.1 阻塞日数 + §9 决策点 D1-D5 累计

## 0914 关键观察(给 T1 拍板)

| 信号 | 数据 | 含义 |
|---|---|---|
| 0913 双漏 | T4 cron + 巡检 cron 同时漏跑 1 日 | 0830 mavis daemon 单次偶发已 12 日稳定,0913 再次单次偶发但范围扩到巡检 |
| 0914 自举 | 本 cron 09-14 03:50 正常触发 | T4 工作流未被 0913 双漏影响,v3.6 节奏 4/5「T4 与任何截止线解耦」扩展到「T4 与巡检偶发解耦」 |
| 节奏 1 第 7 例 | 0914 闭环 0914 plan | 巡检后 plan 文档闭环小节奏稳定,v3.6 节奏 1 待观察 100% 兑现 |
| 节奏 2 第 7 段 | 0913 「双漏」= 新故障类型 | 沿用 v3.6 节奏 2 5 触发 1 未触发,0914 第 7 段为「未触发 + 巡检也未触发」复合 |
| 节奏 6 新增 | 「T4 + 巡检双漏」对 T4 工作流的影响 | T4 工作流定性升级到「与 T1 截止线 / 巡检偶发 / 软硬截止 / 任何外部触发全解耦」 |
| B4 失效 14 次 | 0913 daily 跑出第 20 次 + 0914 daily 跑出第 21 次(预) | 0831 承诺 + 0907/0908/0909/0910 软截止线五失效后,「双漏」后 B4 失效累计再 +1,但 T4 不依赖 B4 信号 |
| 0912 T1 集中决策连续 13 个工作日(0902-0914) | 决策模式「集中决策,不分批」再验证 1 日 | T4 与 T1 决策节奏完全脱钩,T1 不动作不影响 T4 输出 |

**T1 拍板路径建议**(沿用 v3.6 §九,优先级未变):D1(1 条 cron 命令)→ D2(1 个 git 操作)→ D5(查 MapStage.zip)→ D3(数据迁移决策)→ 解锁 D4。

## 下一步候选(留给 0915 T4 或 T1 拍板)

- **T1 拍板(优先级最高,沿用 v3.6)**:D1-D5 决策点,详见 `cities_db_inspect.py §9` v3.2
- **T4 拍板后立即可做**:跑通 1 城市铜陵(340700)端到端 desc_short 灌入(决策表 §五 checklist 第 3-4 项)
- **T4 不该做**:批量入库 / 引 FastAPI(等 D3 + D4 拍板)
- **T4 工作纪律**:本次 0913 双漏属「T4 + 巡检 cron 同时单次偶发」,已入档节奏 6;后续 T4 仍按 0902/0909 模式自举
- **0914 巡检补做**:T4 本 cron 同时也是 0914 巡检(本日巡检内容已并入本 plan)

---

> **T4 工作流定性 v3.7 升级**:新阶段 = 「巡检后 plan 文档闭环小节奏」(7 例形成) + 「自举入库 + plan 闭环」双 commit 模式(4 例形成沿用 0911) + 「T4 + 巡检 cron 双漏」对 T4 工作流无影响(新增节奏 6 验证)三视角构成「T4 工作流在所有外部信号全失效下仍稳定输出」的全貌。
